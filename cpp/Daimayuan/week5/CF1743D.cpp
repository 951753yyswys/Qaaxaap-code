#include<bits/stdc++.h>
#define int long long
using namespace std;
namespace Qaaxaap
{
	void work()
	{
		int n;
		string s,ans;
		cin>>n>>s;
		s='+'+s;
		int len=0;
		for(int i=1;i<=n;i++) 
		{
			if(s[i]=='1') len++;
			else break;
		}
		for(int k=0;k<=len;k++)
		{
			for(int i=1;i<=n;i++)
			{

			}
		}
	}
}
signed main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr),cout.tie(nullptr);
	return Qaaxaap::work(),0;
}

