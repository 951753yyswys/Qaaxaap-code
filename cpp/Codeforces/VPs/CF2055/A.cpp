#include<bits/stdc++.h>
using namespace std;
#define int long long
signed main()
{
    ios::sync_with_stdio(0);
    cin.tie(0),cout.tie(0);
    int t;
    cin>>t;
    while(t--)
    {
        int n,a,b;
        cin>>n>>a>>b;
        if(a%2==b%2) cout<<"yes";
        else cout<<"no";
        cout<<endl;      
        //vector<int>
    }
    return 0;
} 