#include <bits/stdc++.h>
#define int long long
using namespace std;
namespace Qaaxaap {
int fa[101010];
vector<int> void work() {
    int n;
    cin >> n;
    for (int i = 1; i < n; i++) {
        int u, v;
        cin >> u >> v;
    }
}
} // namespace Qaaxaap
signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr), cout.tie(nullptr);
    return Qaaxaap::work(), 0;
}
